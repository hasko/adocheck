# ADOit API Credentials
ADOIT_URL=https://your-adoit-instance.example.com
ADOIT_API_ID=your_api_id
ADOIT_API_SECRET=your_api_secret
ADOIT_REPO_ID=your_repository_id  # Can be with or without curly braces, e.g., {uuid} or uuid

# Optional: Proxy settings (uncomment if needed)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# Optional: Cache TTL in seconds (default: 172800 = 48 hours)
# Set to 0 to always check for modifications (slower but guaranteed fresh)
# Set to higher value for more aggressive caching
# ADOIT_CACHE_TTL=172800